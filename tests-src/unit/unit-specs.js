import {
  assert,
  modalRouter,
  stateHandler,
  utils,
} from './loader.js';


describe('My Module', function () {
  console.dir({
    assert,
    modalRouter,
    stateHandler,
    utils,
  });

  'use strict';
  xit('initialises correctly');
});
